import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'progress-bars__ripple-expand',
  title: 'Ripple Expand',
  description: 'Achievement impact feedback through expanding ripple rings emanating from progress bar at milestones.',
  tags: ['css', 'js'],
}
