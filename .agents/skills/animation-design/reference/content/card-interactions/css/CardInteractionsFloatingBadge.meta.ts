import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'card-interactions__floating-badge',
  title: 'Orbital Notification',
  description: 'A badge that floats independently with magnetic physics and live activity indicators.',
  tags: ['css', 'hover', 'badge', 'premium'],
}