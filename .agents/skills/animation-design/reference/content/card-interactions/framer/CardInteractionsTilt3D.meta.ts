import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'card-interactions__tilt-3d',
  title: 'Parallax Glare',
  description: 'Heavy physics tilt with dynamic glare reflection and depth.',
  tags: ['framer', 'hover', '3d', 'premium'],
}