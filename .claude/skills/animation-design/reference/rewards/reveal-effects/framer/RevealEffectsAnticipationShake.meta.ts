import type { AnimationMetadata } from '@/types/animation'

export const metadata = {
  id: 'reveal-effects__anticipation-shake',
  title: 'Anticipation Shake',
  description: 'Building tension shake that increases in intensity before reveals',
  tags: ['framer'],
} satisfies AnimationMetadata
