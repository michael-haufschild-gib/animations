import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'icon-animations__badge-pop-rotate',
  title: 'Badge Pop Rotate',
  description: 'Badge pops in with scale bounce and rotation for achievement reveals',
  tags: ['css'],
}
