import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'progress-bars__glow-trail',
  title: 'Glow Trail',
  description: 'Energy/power charging indicator with pulsing glow at the leading edge of progress fill.',
  tags: ['framer'],
}
