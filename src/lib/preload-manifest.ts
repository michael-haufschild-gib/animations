// Import assets so Vite resolves and fingerprints them in production

// Extendable: add more assets here as needed
import { homeIcon1, homeIcon2, presentBox, presentBoxBalloon, pulseScroll, shakeIcon } from '@/assets'
export const CRITICAL_ICON_IMAGES: string[] = [
  presentBoxBalloon,
  presentBox,
  shakeIcon,
  pulseScroll,
  homeIcon1,
  homeIcon2,
]
