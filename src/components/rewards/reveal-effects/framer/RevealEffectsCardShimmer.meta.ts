import type { AnimationMetadata } from '@/types/animation'

export const metadata = {
  id: 'rewards-reveal-effects__card-shimmer',
  title: 'Card Shimmer Reveal',
  description: 'Card appears with a diagonal light shimmer effect.',
  tags: ['card', 'shimmer', 'light', 'reveal'],
} satisfies AnimationMetadata
