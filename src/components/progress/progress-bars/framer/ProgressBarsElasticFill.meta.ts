import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'progress-bars__elastic-fill',
  title: 'Elastic Fill',
  description: 'Playful progress bar fill that overshoots, squashes during compression, and settles with elastic bounce physics.',
  tags: ['framer'],
}
