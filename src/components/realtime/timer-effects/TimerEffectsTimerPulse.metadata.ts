import type { AnimationMetadata } from '@/types/animation'

export const timerEffectsTimerPulseMetadata: AnimationMetadata = {
  id: 'timer-effects__timer-pulse',
  title: 'Pulse Urgency',
  description: 'Last seconds pulse to draw focus.',
  tags: ['js', 'css'],
}

export default timerEffectsTimerPulseMetadata
